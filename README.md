# forgetmenot
Repository for ForgetMeNot, a webapp that lets the user save notes or input vague commands and utilizing AI it will determine which of it's functions to run. Frontend written in typescript using the Angular framework
